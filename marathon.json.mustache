{
  "id": "{{service.name}}",
  "cpus": {{service.cpus}},
  "mem": {{service.mem}},

  "env": {
    "DCOS_SERVICE_NAME": "{{service.name}}",
    "_CLIENT_TM_COUNT": "{{service.taskmanager.count}}",
    "_CLIENT_TM_MEMORY": "{{service.taskmanager.memory}}",
    "_SLOTS": "{{service.slots}}",
    "_CLIENT_USERNAME": "{{service.user}}",
    "_CLIENT_USERNAME": "{{service.session_id}}",
  },

  "container": {
    "type": "DOCKER",
    "docker": {
      "image": "{{resource.assets.container.docker.flink}}",
      "network": "HOST",
      "forcePullImage": true
    }
  },

  "healthChecks": [
    {
      "protocol": "HTTP",
      "path": "/",
      "portIndex": 0,
      "gracePeriodSeconds": 5,
      "intervalSeconds": 60,
      "timeoutSeconds": 10,
      "maxConsecutiveFailures": 3
    }
  ]
}
